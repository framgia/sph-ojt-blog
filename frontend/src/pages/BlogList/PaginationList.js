import React from 'react';
import { Pagination } from 'semantic-ui-react';

const PaginationList = () => {
	return <Pagination defaultActivePage={4} totalPages={4} />;
};

export default PaginationList;
